<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="IRCameraView.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IRCameraView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Image x:Name="imageElement" Stretch="Uniform" />

        <Grid>
            <StackPanel Spacing="10" Margin="10" Orientation="Horizontal">
                <ComboBox x:Name="DeviceComboBox" SelectionChanged="DeviceComboBox_SelectionChanged" Width="300" Margin="10"/>
                <ComboBox x:Name="CameraType" ItemsSource="" SelectionChanged="CameraType_SelectionChanged" Width="300" Margin="10">
                    <ComboBoxItem>Infrared</ComboBoxItem>
                    <ComboBoxItem>Color</ComboBoxItem>
                    <ComboBoxItem>Depth</ComboBoxItem>
                </ComboBox>
              <Button Click="TakePhoto_Click">Photo</Button>
                <ToggleButton Click="TakeVideo_Click">Video</ToggleButton>
                <TextBlock VerticalAlignment="Center">00:00</TextBlock>
            </StackPanel>
            <StackPanel Spacing="10" Margin="10" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Frame filter" VerticalAlignment="Center"/>
                <ComboBox SelectedIndex="0" SelectionChanged="FrameFilter_SelectionChanged">
                    <ComboBoxItem>None</ComboBoxItem>
                    <ComboBoxItem>Dark</ComboBoxItem>
                    <ComboBoxItem>Illuminated</ComboBoxItem>
                </ComboBox>
                <TextBlock Text="Vision" VerticalAlignment="Center"/>
                <ComboBox SelectedIndex="0" SelectionChanged="ComboBox_SelectionChanged">
                    <ComboBoxItem>Standard</ComboBoxItem>
                    <ComboBoxItem>Night</ComboBoxItem>
                </ComboBox>
                <!--<Grid Name="AdvancedSettingsPanel">
                </Grid>-->
                <ListView Name="AdvancedSettingsList"></ListView>
                <Grid Name="PhotoModeGrid" Visibility="Collapsed">
                    <TextBlock Text="Photo Mode" VerticalAlignment="Center"/>
                    <ComboBox Name="PhotoModeBox"  SelectedIndex="0" SelectionChanged="PhotoModeBox_SelectionChanged"/>
                </Grid>
                <Grid Name="TorchControlGrid" Visibility="Collapsed">
                    <TextBlock Text="IR Torch" VerticalAlignment="Center"/>
                    <ComboBox Name="IRTorchBox"  SelectedIndex="0" SelectionChanged="IRTorchBox_SelectionChanged"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
